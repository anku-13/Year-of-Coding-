int countBuildings(int arr[], int n){
    // Don't write main().
    // Write your code here.
  int count=1;
  int max=arr[0];
  for(int i=0;i<n;i++)
  {
    if(arr[i]>max)
    {
      count++;
      max=arr[i];
    }
  }
  return count;
}
