#include<bits/stdc++.h>
void findWinner(int * arr, int n ){
	//code - here
int xr=0;
  for(int i=0;i<n;i++)
  {
    xr^=arr[i];
  }
  if(n%2==0 || xr==0)
  {
    cout<<"First\n";
  }
  else
    cout<<"Second\n";
}
